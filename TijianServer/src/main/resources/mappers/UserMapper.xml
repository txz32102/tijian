<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shou.tijianserver.mapper.UserMapper">
<sql id="ids">
userId,
password,
realName,
gender,
identityCard,
birthday,
userType
</sql>
<select id="selectUser" resultType="User">
    select <include refid="ids"/> from user where userId=#{userId} and password=#{password}
</select>
</mapper>